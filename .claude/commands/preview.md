---
description: Start local dev server to preview the site
allowed-tools:
  - Bash
---

Start the development server to preview the site locally.

## Instructions

1. Check if dependencies are installed (node_modules exists)
2. If not, run `npm install` first
3. Start the dev server: `npm run dev`

## Output

```
🚀 Starting preview server...

Server running at: http://localhost:4321/digital-garden/

Press Ctrl+C to stop the server.
```

## Notes

- The server will hot-reload when you make changes
- Preview URL includes the base path (/digital-garden/)
- Keep the terminal open while previewing
